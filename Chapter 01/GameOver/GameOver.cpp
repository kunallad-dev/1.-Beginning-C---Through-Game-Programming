#include<iostream>

void displayGameOverMessage() {
    std::cout << "Game Over! Thanks for playing." << std::endl;
}

int main() {
    displayGameOverMessage();
    return 0;
}

// This code defines a simple C++ program that displays a "Game Over" message to the console.
// It uses the C++17 standard and includes a function to encapsulate the message display logic.
// The main function calls this display function when the program is executed.
